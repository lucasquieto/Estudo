using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S07A39 : MonoBehaviour {

    public string[] fruta;

	// Use this for initialization
	void Start () {

        // Sintaxe
        foreach(string e in fruta) {
            print(e);
        }

	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
